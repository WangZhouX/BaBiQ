# P8 画布编排原型审查记录（2026-06-12）

> 审查对象：Figma「总原型UI」`frTp55zgrKf4NAWxn6LdI7`，section `395:2`，五帧：
> P8 01 创建编排容器 `393:2` / P8 02 添加结构与节点 `393:126` / P8 03 节点编辑与保存 `393:319` / P8 04 启动与审批 `393:512` / P8 05 运行态回放 `393:709`。
> 结论：整体质量高，五帧叙事完整，作为 P8 视觉基准采纳；下列待改项分级处理。
> 计划侧已同步的修订：`plan.md` D9/D10 修订 + D14（视觉通道分离）/ D15（锚定浮层）/ D16（无拖拽开关、组标签、不做侧卡）。

## 原型优于原计划、已回写采纳的方案

- ✅ **线上 "+" 插入菜单**（帧2）：连线上点 "+" 弹出"添加到此处"菜单，插入位置所见即所得 → 已回写 D10，实现以此为主、工具栏按钮降为"追加到末尾"。
- ✅ 校验状态闭环（空徽标 + 顶部提示 + 启动置灰 → 校验通过 → 启动可用）、approve-once 弹窗按组分段 + 写入范围高亮、目标独立区 + START/END 端点化、未保存/已保存 dirty chip、嵌套组包络框 + "组内不嵌组"标注 —— 全部符合计划，保持。

## A 类：必须修（影响实现语义，原型与实现都要改）

| # | 帧 | 问题 | 修改要求 |
|---|---|---|---|
| A1 | 全部，尤其 `393:709` | 角色色（reviewer 红、writer 橙…）占用了颜色通道，运行态状态只剩小徽标；常驻红色 reviewer 会被误读为"失败" | 按 D14：节点主体中性底色 + 左侧角色小色点；颜色通道留给运行状态（pending 灰 / running 强调色呼吸 / completed 绿 / failed 红）；红色全局仅用于危险与失败 |
| A2 | `393:709` | 回放态残留编辑入口：线上 "+"、探查/实现/验证模板按钮、"拖拽结构"开关均可见，"启动"按钮仍为可点蓝色 | 回放态隐藏全部编辑 affordance；启动按钮改"运行中"禁用态（D9） |
| A3 | `393:319` | 节点设置浮层固定在画布右上：与被编节点（中下部 writer）距离远、遮挡 tester；且缺"工具模式（只读/工作区写入）"与"重命名"字段 | 改锚定浮层：跟随选中节点、带指向、自动避让；补 mode 与重命名字段（D15，审批写入范围依赖 mode） |
| A4 | 全部 | 插入菜单与工具栏均无"路由组"与"自定义"模板入口 | 菜单补全：探查/实现/验证/自定义 + 并行组/路由组（D10） |

## B 类：应该修（语义与可读性）

| # | 帧 | 问题 | 修改要求 |
|---|---|---|---|
| B1 | 全部 | "编辑"模式激活态用红色描边 | 改主题强调色；红色让给危险/失败（D14） |
| B2 | `393:126` 等 | 组标签直接显示技术 id "并行组 g_par_1" | 显示"并行组"或自定义组名；groupId 进 tooltip（D16） |
| B3 | `393:512` | 审批弹窗结构文本为技术面包屑 `root(SEQUENTIAL): …` | 口语化分步 + 每节点标注读/写；技术串折叠进详情（已写入 plan Task 4） |
| B4 | 全部 | "拖拽结构"toggle 开关 | 删除；固定手势分工：拖节点=结构操作、拖空白=平移（D16） |
| B5 | `393:709` | 右上"运行回放"侧卡与画布状态重复，且粒度不一致（列组不列子节点） | 删除侧卡（画布即状态显示器）；如需事件明细，后续做带时间戳事件流（D16，本期不做） |
| B6 | `393:709` | 运行中的 writer 仍挂"空"徽标 | 状态徽标互斥：启动校验通过后"空"徽标不可能出现（plan Task 7 已加断言） |
| B7 | `393:2` | END 胶囊与"START/END 是端点…"提示文字重叠 | 文案移到端点下方或 tooltip |
| B8 | `393:126` | "添加到此处"菜单遮挡 tester 节点 | 菜单定位加避让（实现注意 z 序与边界翻转） |

## C 类：建议补帧（不阻塞，但实现容易跑偏的状态）

1. **失败态帧**：某节点 failed 红框 + 错误摘要 + 下游节点中止灰态 + 流程状态"failed"（plan 烟测 #5 的视觉基准）。
2. **窄分屏降级帧**：分屏 <600dp 时回退现有紧凑节点列表（plan Task 9）。
3. "搜索节点"底部链接：本期为禁用占位（capability 目录接入是范围外），原型加置灰态标注，与 P1-4 占位惯例一致。
