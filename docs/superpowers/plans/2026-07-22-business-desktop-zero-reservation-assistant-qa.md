# 业务桌面零占位助手验收记录

## 1. 验收结论

2026-07-22，业务桌面“零占位助手”完成自动化验收和真实 UI 人工验收。

验收结果：通过。助手收起时不再保留整条右侧占位区域；展开时与业务区相邻布局并正常推挤业务表单；左侧导航、左下设置入口、分隔条调整和再次收起均符合预期。

## 2. 验收范围

- 移除业务桌面原有的 52dp Compose 顶栏。
- 将设置入口固定到左侧导航底部。
- 助手收起时仅保留右下角吉祥物，不预留右侧栏宽度。
- 助手展开时在业务区右侧相邻显示，并支持通过分隔条调整宽度。
- 更新打包烟测和布局契约，覆盖新的窗口组合方式。

## 3. 自动化验收

验证环境：

- `JAVA_HOME=D:\Program Files\jdk21`
- `GRADLE_USER_HOME=C:\tmp\gradle-home`
- `TEMP=C:\tmp\gradle-temp`
- `TMP=C:\tmp\gradle-temp`
- Gradle 使用 `--no-daemon` 和 `-Pkotlin.compiler.execution.strategy=in-process`

### 3.1 聚焦验证

聚焦验证包含布局策略、桌面 Shell、侧栏、附件工作流、打包烟测和分发构建逻辑测试。

- 退出码：`0`
- 耗时：`137.359s`
- XML 统计：`45 tests / 0 failures / 0 errors / 0 skipped`
- 结果：`45/45` 通过
- 日志：`C:\tmp\babiq-zero-reservation-focused.log`

该命令运行 `:app:test`，因此本轮聚焦统计均来自 `app`，未运行 `agent-client-core` 测试。

### 3.2 桌面全量验证

- 退出码：`0`
- 耗时：`253.372s`
- `app`：`227 tests / 0 failures / 0 errors / 0 skipped`
- `agent-client-core`：`147 tests / 0 failures / 0 errors / 0 skipped`
- 合计：`374/374` 通过
- 日志：`C:\tmp\babiq-zero-reservation-full.log`

已知可能偶发的 `BusinessDesktopFrameworkIT` 快速 `EXECUTING -> FAILED` 丢失 running 状态问题本次未发生；该测试类的 3 个测试全部通过。验证过程中未重试、未增加延时，也未放宽断言。

### 3.3 仓库检查

- `git diff --check 9d31a8c..HEAD`：退出码 `0`，无空白错误。
- 自动化验收结束时 `git status --short` 为空。
- 自动化验证未修改代码、测试或文档。

## 4. 真实 UI 人工验收

### 4.1 启动环境

- 使用 JDK 21 和 framework demo identity 启动 `:app:run`。
- 使用独立临时 `user.home`：`C:\tmp\babiq-zero-reservation-uiqa-home-2`。
- 成功完成交互验收的进程使用上述隔离 Home，未覆盖用户实际 Home。
- 在此之前有一次失败的后台启动未继承临时 `user.home`，曾只读校验用户 Home 中现有数据库；由于数据库 schema 版本更新，进程未执行迁移、未覆盖数据并安全退出，随后才改用隔离 Home 完成验收。
- 窗口标题为“翔鸟律智桌面端”。
- 窗口启动后即为最大化状态，标题栏按钮显示“还原”。
- 验收截图分辨率为 `1920x1032`。

### 4.2 交互检查

1. 初始收起状态
   - 原生标题栏下方立即进入全高左右主体，没有 52dp Compose 顶栏。
   - 左栏顶部依次显示工作台、资料录入、运行记录，设置入口位于左下角。
   - 业务表单延伸到窗口右边缘，没有灰色或白色的整条右侧占位栏。
   - 右下角仅显示吉祥物自身。

2. 左侧导航
   - 点击左下设置后进入 Provider 设置页，设置入口显示高亮。
   - 点击资料录入后可以正常返回业务表单。

3. 助手展开
   - 点击右下角吉祥物后，“小律智能助手”在业务区右侧相邻展开。
   - 业务表单被正常推挤，没有被助手覆盖。
   - 左侧导航和左下设置入口继续保留。

4. 分隔条调整
   - 将分隔条从约 `x=1459` 拖动到 `x=1360`。
   - 助手宽度增加约 `99px`，拖动调整生效。

5. 助手再次收起
   - 点击展开面板内的吉祥物后，助手正常收起。
   - 业务区恢复延伸至窗口右边缘，没有重新出现整条右侧占位。

### 4.3 退出与残留检查

- 关闭窗口后，`:app:run` 退出码为 `0`。
- Gradle 输出 `BUILD SUCCESSFUL in 1m 46s`。
- 日志中仅出现 Skiko DirectX 12 初始化失败后的 fallback warning，实际窗口渲染和交互正常。
- `jps -lv` 对 `MainKt`、`babiq-server` 和该隔离工作树均无命中，没有前后端进程残留。

## 5. 最终功能提交

- `c838026 fix(桌面): 收起助手改为零占位悬浮入口`
- `7c1b27f feat(桌面): 将设置固定到左侧导航底部`
- `b7b27b7 test(桌面): 烟测改为验证左侧导航组合`
- `4a90442 refactor(桌面): 移除多余顶部设置工具栏`

## 6. 边界说明

本记录区分自动化验收与真实 UI 人工验收：自动化测试证明布局、Shell、侧栏、协议和相关构建契约通过；真实 UI 人工验收另行确认最大化窗口中的实际布局、导航、展开、拖动、收起及进程退出行为。
