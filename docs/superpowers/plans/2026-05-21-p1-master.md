# BaBiQ P1 Implementation Plan (Master)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **This is the MASTER plan** — a navigation document that scopes P1 across 4 sub-systems. **Detailed task-level plans (with TDD steps) live in separate documents under `docs/superpowers/plans/p1-*/`** and will be authored just before execution.

**Goal:** 实现一个对标 OpenAI Codex 桌面端的 AI Agent 学习项目最小可用版本(P1):桌面端能多轮对话、调用本地工具、走完整审批流、可切换多个 LLM Provider。

**Architecture:** 跨进程 Client/Server。Kotlin Compose Desktop + Ktor WebSocket 客户端,通过 JSON-RPC 2.0 over WebSocket 连接 Spring Boot Agent Server。后端基于 Spring AI Alibaba + Spring AI(OpenAI 兼容)双工厂,围绕 `Thread/Turn/Item` 状态机实现 ReAct 主循环。

**Tech Stack:** Java 25, Spring Boot 3.5.14, Spring AI Alibaba 1.1.2.x, Kotlin 2.0, Compose Multiplatform 1.6+, Ktor 2.3+, kotlinx.serialization, Jackson, WebSocket, JSON-RPC 2.0.

**Architecture Reference:** [docs/ARCHITECTURE.md](../../ARCHITECTURE.md)

---

## 1. P1 范围边界 (Scope Lock)

### ✅ P1 必须做

| 类别 | 内容 |
|---|---|
| 协议 | WebSocket + JSON-RPC 2.0(单连接、单 Thread) |
| 状态机 | Thread / Turn / Item 三层模型,Turn 五态(CREATED / RUNNING / WAITING_APPROVAL / COMPLETED / FAILED / CANCELED) |
| Item 类型 | 接口预留 12 种,**P1 实现 6 种**: `userMessage` / `agentMessage` / `reasoning` / `plan` / `commandExecution` / `fileChange` |
| 模型 | 双工厂: `DashScopeProviderFactory` + `OpenAiCompatibleProviderFactory`(覆盖 DeepSeek/OpenAI/Azure/OneAPI/Ollama) |
| Agent Loop | ReAct: 模型决策 → 工具调用 → 审批 → 执行 → 回灌 |
| 工具 | 6 个: `read_file` / `write_file` / `exec_shell` / `list_dir` / `grep` / `apply_patch` |
| 审批 | 三档策略: `never` / `on-request` / `on-failure`,响应支持 `turn` / `session` scope |
| 沙箱 | `read-only` / `workspace-write` / `danger-full-access` 三档,P1 用纯 Java `PathGuard` 实现路径白名单 |
| 桌面端 | Compose Desktop:ChatScreen + ApprovalDialog + Provider 切换下拉 + 设置面板 |
| 持久化 | **P1 全内存**,无 SQLite |

### ❌ P1 不做(明确推到 P2+)

- 多 Thread / 历史会话恢复
- SQLite 持久化
- MCP Client / Server 集成
- 多模态(图片/文件拖拽)
- 上下文压缩(`contextCompaction`)
- Review Mode 只读快照
- OS 级真沙箱(容器/jail)
- CI/CD、可观测性、自动化集成测试套件
- Battle 模式(多 provider 并排对比)

---

## 2. 子系统拆分与依赖关系

```
                    ┌──────────────────────────────┐
                    │  P1-0  Monorepo 骨架 + 公共配置 │  (前置,0.5 天)
                    └──────────────┬───────────────┘
                                   │
              ┌────────────────────┼────────────────────┐
              ▼                                         ▼
   ┌──────────────────────┐                ┌──────────────────────┐
   │  P1-1  协议层           │                │  P1-2  Provider 层    │
   │  WebSocket + JSON-RPC  │                │  ChatClientFactory   │
   │  Thread/Turn/Item 模型 │                │  DashScope + OpenAI  │
   │  (2-3 天)              │                │  Compatible 双工厂    │
   │  独立验证: wscat       │                │  (2 天)               │
   └──────────┬───────────┘                 │  独立验证: REST 端点  │
              │                              └──────────┬──────────┘
              └────────────────────┬────────────────────┘
                                   ▼
                    ┌──────────────────────────────┐
                    │  P1-3  Agent Loop + 工具/审批  │
                    │  ReAct 循环 + 6 工具 + 沙箱     │
                    │  (4-5 天)                     │
                    │  独立验证: wscat + 真模型      │
                    └──────────────┬───────────────┘
                                   ▼
                    ┌──────────────────────────────┐
                    │  P1-4  Compose Desktop UI      │
                    │  ChatScreen + ApprovalDialog   │
                    │  + Provider 下拉 + 设置面板    │
                    │  (3-4 天)                     │
                    └──────────────────────────────┘

总预估: 12-15 个工作日
```

### 依赖详解

- **P1-1 与 P1-2 可并行**(没有代码依赖)
- **P1-3 依赖 P1-1 的 JSON-RPC 框架和 P1-2 的 ChatClientFactory**
- **P1-4 必须在 P1-3 跑通后才开始**(否则桌面端没东西连)
- 每个子计划完成后**强制走 verification-before-completion**,跑通才能进下一阶段

---

## 3. 文件结构(Monorepo)

> 这是 P1 终态。每个子计划只会触碰自己范围内的文件。

```
BaBiQ/
├── README.md
├── pom.xml                                   # 父 POM (聚合 backend)
├── settings.gradle.kts                       # Kotlin 聚合(desktop)
├── docs/
│   ├── ARCHITECTURE.md
│   └── superpowers/plans/
│       ├── 2026-05-21-p1-master.md           # 本文档
│       ├── p1-0-skeleton/                    # 各子计划的详细 TDD plan
│       ├── p1-1-protocol/
│       ├── p1-2-providers/
│       ├── p1-3-agent-loop/
│       └── p1-4-desktop/
│
├── backend/                                  # Spring Boot Agent Server
│   ├── pom.xml
│   └── src/
│       ├── main/java/com/wzx/babiq/server/
│       │   ├── BaBiQApplication.java
│       │   ├── api/                          # P1-1
│       │   │   ├── JsonRpcWebSocketHandler.java
│       │   │   ├── JsonRpcMessage.java       # sealed:Request/Response/Notification/Error
│       │   │   ├── JsonRpcDispatcher.java    # method → handler 路由
│       │   │   ├── method/                   # 每个方法一个 handler
│       │   │   │   ├── ThreadCreateHandler.java
│       │   │   │   ├── TurnStartHandler.java
│       │   │   │   ├── TurnCancelHandler.java
│       │   │   │   ├── ApprovalRespondHandler.java
│       │   │   │   ├── ProvidersListHandler.java
│       │   │   │   └── ProvidersSetActiveHandler.java
│       │   │   └── error/
│       │   │       └── JsonRpcErrorCode.java
│       │   ├── conversation/                 # P1-1
│       │   │   ├── ConversationService.java
│       │   │   ├── Thread.java
│       │   │   ├── Turn.java
│       │   │   ├── TurnStatus.java           # enum
│       │   │   ├── ItemEmitter.java          # 流式发射 item/* 通知
│       │   │   └── items/                    # 12 种 Item 类
│       │   │       ├── ThreadItem.java       # sealed interface
│       │   │       ├── UserMessageItem.java
│       │   │       ├── AgentMessageItem.java
│       │   │       ├── ReasoningItem.java
│       │   │       ├── PlanItem.java
│       │   │       ├── CommandExecutionItem.java
│       │   │       ├── FileChangeItem.java
│       │   │       └── (其余 6 种为占位,P1 不实现)
│       │   ├── model/                        # P1-2
│       │   │   ├── ModelProviderRegistry.java
│       │   │   ├── ModelProviderConfig.java
│       │   │   ├── ProviderType.java         # enum
│       │   │   ├── ChatClientFactory.java
│       │   │   └── provider/
│       │   │       ├── ProviderFactory.java  # 接口
│       │   │       ├── DashScopeProviderFactory.java
│       │   │       └── OpenAiCompatibleProviderFactory.java
│       │   ├── agent/                        # P1-3
│       │   │   ├── AgentLoop.java
│       │   │   ├── ToolCallParser.java
│       │   │   └── ReAct/
│       │   │       └── ReActStrategy.java
│       │   ├── tool/                         # P1-3
│       │   │   ├── Tool.java                 # 接口 + @Tool 注解约定
│       │   │   ├── ToolRegistry.java
│       │   │   ├── ToolResult.java
│       │   │   └── impl/
│       │   │       ├── ReadFileTool.java
│       │   │       ├── WriteFileTool.java
│       │   │       ├── ExecShellTool.java
│       │   │       ├── ListDirTool.java
│       │   │       ├── GrepTool.java
│       │   │       └── ApplyPatchTool.java
│       │   ├── approval/                     # P1-3
│       │   │   ├── ApprovalEngine.java
│       │   │   ├── ApprovalPolicy.java       # enum
│       │   │   ├── ApprovalDecision.java
│       │   │   ├── ApprovalRequest.java
│       │   │   └── PendingApprovalRegistry.java   # threadId+itemId → CompletableFuture
│       │   ├── sandbox/                      # P1-3
│       │   │   ├── SandboxMode.java          # enum
│       │   │   ├── SandboxPolicy.java
│       │   │   └── PathGuard.java
│       │   └── config/                       # P1-0
│       │       ├── BaBiQProperties.java
│       │       └── WebSocketConfig.java
│       ├── main/resources/
│       │   ├── application.yml
│       │   └── application-dev.yml
│       └── test/java/com/wzx/babiq/server/
│           └── (单元测试,每个子计划带)
│
└── desktop/                                  # Compose Multiplatform Desktop (Kotlin)
    ├── build.gradle.kts
    └── src/main/kotlin/com/wzx/babiq/desktop/
        ├── Main.kt
        ├── client/                            # P1-4
        │   ├── AgentClient.kt
        │   ├── ProtocolModels.kt              # 镜像后端 Item DSL
        │   └── JsonRpcCodec.kt
        ├── ui/                                # P1-4
        │   ├── ChatScreen.kt
        │   ├── ApprovalDialog.kt
        │   ├── SettingsScreen.kt
        │   ├── ProviderSelector.kt
        │   └── components/
        │       ├── MessageBubble.kt
        │       ├── CommandPreview.kt
        │       └── DiffViewer.kt
        └── state/                             # P1-4
            └── ChatViewModel.kt
```

---

## 4. 子系统里程碑与验收标准

> 每个子系统的验收标准 = 进入下一阶段的硬门槛。

### M0 — P1-0 Monorepo 骨架

**目标:** 把当前单 module 项目重构成 Monorepo,Maven `backend/` + Gradle `desktop/`,各自可以独立 build。

**验收:**
- `cd backend && ./mvnw clean package` 成功,产出 `backend/target/*.jar`
- `cd desktop && ./gradlew assemble` 成功
- `cd backend && ./mvnw spring-boot:run` 能启动到 "Started BaBiQApplication"
- 当前 `src/` 已迁移到 `backend/src/`,旧路径清空
- README.md 更新跑命令

### M1 — P1-1 协议层(可独立验证)

**目标:** WebSocket + JSON-RPC 2.0 框架跑通,实现 `thread/create` + `turn/start` 握手,server 能推 mock item(不接真模型)。

**验收:**
- `wscat -c ws://localhost:8080/ws/agent` 能连上
- 发 `{"jsonrpc":"2.0","method":"thread/create","id":1,"params":{"cwd":"."}}`,收到 `{"id":1,"result":{"threadId":"thr_xxx"}}`
- 发 `turn/start`,顺序收到 `turn/started` → `item/added` (userMessage) → `item/added` (agentMessage,mock 文本"hello from babiq") → `turn/completed`
- Turn 状态机 5 个状态全部有单测覆盖
- 错误码符合 JSON-RPC 2.0 规范(`-32601` method not found / `-32602` invalid params / `-32000` server error)

### M2 — P1-2 Provider 层(可独立验证)

**目标:** 多 Provider 配置体系完成,REST 测试端点能切换 4 类 provider 跑通 hello world。

**验收:**
- `application.yml` 配置 4 个 provider: DashScope / DeepSeek / 一个 OneAPI 中转 / Ollama
- 启动后 `GET /api/test/providers` 返回 4 个 provider 列表
- `POST /api/test/chat?providerId=dashscope-default` body `{"text":"hi"}` 返回真实模型回复
- 同上 POST 切到 `deepseek-official` 也能返回回复
- `ChatClientFactory` 有针对 DashScope / OpenAI Compatible 两类的单测
- 缺失 api-key 时给出明确错误而不是 NPE

### M3 — P1-3 Agent Loop + 工具 + 审批 + 沙箱

**目标:** 整合 P1-1 + P1-2,完整 ReAct 循环跑通,wscat 输入"读 README 并总结"能完成端到端流程。

**验收:**
- 6 个工具全部实现 + 单测
- ReAct 循环至少执行 2 轮工具调用不死循环(有 max-iterations 兜底)
- `read_file` / `list_dir` / `grep` 无审批直接执行
- `write_file` / `exec_shell` / `apply_patch` 在 `on-request` 策略下触发 `approval/request`
- wscat 客户端能用 `approval/respond` 回 approve/deny,行为符合预期
- 用 `read-only` 沙箱时 `write_file` 立刻拒绝并发 `item/added` (fileChange, status=denied)
- 实际场景: wscat 发 "读取项目根目录下的 README.md 并用 3 句话总结",收到 fileChange + agentMessage,流程完整结束

### M4 — P1-4 Compose Desktop UI

**目标:** 桌面端跑起来连后端,完成 M3 同样的"读 README 并总结"任务,过程中弹审批。

**验收:**
- `./gradlew :desktop:run` 拉起桌面窗口
- 输入框打字 + 回车发送,聊天气泡流式渲染
- shell 执行触发审批弹窗,显示命令 + Approve/Deny/Always 按钮
- 顶部 Provider 下拉切换,下条消息走新模型
- 设置面板能看到所有 provider 信息(P1 只读,不做编辑)
- 实际场景: 完整完成"分析 X 项目结构并写一个总结" 任务,过程肉眼可见

---

## 5. 全局技术决策(防止跑偏)

> 这些决策**在所有子计划中保持一致**。变更前必须先更新本节。

| 编号 | 决策 | 选择 | 原因 |
|---|---|---|---|
| D1 | JSON-RPC 框架 | **自己写**,不引第三方 | Spring 生态没成熟的 JSON-RPC,自写 200 行可控,且是学习重点 |
| D2 | WebSocket 实现 | spring-boot-starter-websocket(原生) | 不要 Netty 直接裸写,要享受 Spring 自动装配 |
| D3 | Item 模型类型 | Java sealed interface + records | Java 25 原生支持,匹配 tagged union |
| D4 | Provider 配置加载 | `@ConfigurationProperties("babiq")` + List | 类型安全,IDE 补全好 |
| D5 | Provider mutate | OpenAiApi.mutate().baseUrl().build() | Spring AI 官方支持,无需 hack |
| D6 | 工具描述给 LLM | Spring AI 的 `@Tool` 注解 + `MethodToolCallbackProvider` | 跟随 Spring AI 1.0 GA 标准 |
| D7 | 工具 schema | 通过反射从方法签名自动推断 | 不要手写 JSON schema |
| D8 | 审批等待机制 | `CompletableFuture<ApprovalDecision>` + 注册表 | server 发请求后 await,client 回响应时 complete |
| D9 | 流式输出 | Reactor `Flux<String>` → 拆 token → 多次 `item/updated` | Spring AI 原生支持 reactive |
| D10 | 单元测试 | JUnit 5 + AssertJ + Mockito 5 | Spring Boot 3 默认 |
| D11 | 集成测试 | `@SpringBootTest` + 真启动 WebSocket | 协议层必须端到端测 |
| D12 | 配置文件格式 | `application.yml`(不用 properties) | 嵌套 provider 数组 |
| D13 | 桌面端 JSON | kotlinx.serialization | 与 Compose 生态原生匹配 |
| D14 | 桌面端 WebSocket | Ktor Client + WebSockets 插件 | 与 Compose 同 Kotlin 生态 |
| D15 | Git 策略 | 主分支保护,每个子计划 `feat/p1-x-xxx` 分支 + 合并 PR | 学习过程要有清晰历史 |
| D16 | 提交粒度 | 每个 TDD 步骤一个 commit | frequent commits 原则 |
| D17 | 日志格式 | Logback,JSON 结构化输出(便于后期接 ELK) | 工程化基础 |

---

## 6. 风险与缓解

| 风险 | 严重度 | 缓解 |
|---|---|---|
| **Java 25 + Spring Boot 3.5 兼容性** | 中 | 子计划 P1-0 第一步:跑通 `mvn spring-boot:run`,失败立即降级到 JDK 21 |
| **Spring AI 1.0 GA 与 Spring AI Alibaba 1.1.2.x API 漂移** | 中 | 锁定版本,所有依赖加 `<dependencyManagement>` BOM |
| **流式 token 拆包导致 UI 闪烁** | 中 | Reactor backpressure + 客户端 buffer 拼接,P1-1 设计阶段就要约定字符边界 |
| **审批 `CompletableFuture` 超时悬挂** | 高 | 强制 timeout(默认 5 分钟),超时自动 deny + 发 turn/failed |
| **Compose Desktop Windows 10 兼容性** | 低 | 撞到时再换 JavaFX 兜底 |
| **真 LLM 调用产生费用** | 低 | dev 环境优先用 Ollama 本地模型;集成测试用 mock provider |
| **沙箱被绕过(符号链接、路径规范化)** | 高 | `PathGuard` 必须用 `Path.toRealPath()` + 白名单前缀比较,**单测覆盖所有绕过尝试** |

---

## 7. 进入下一阶段的流程

```
本 master plan ✅ (你正在读)
       │
       ▼
确认 → 我用 writing-plans 为 P1-0 写详细 TDD plan
       │  (步骤级、含代码、含命令)
       ▼
你确认 P1-0 plan
       │
       ▼
用 OMC ultrawork / subagent-driven-development 执行 P1-0
       │
       ▼
verification-before-completion 验收 M0
       │
       ▼
进入 P1-1 → 写详细 plan → 执行 → 验收 M1 → ...
```

**每次只让你看一份不超过 300 行的子计划**,避免认知过载。

---

## 8. 立即下一步

待你确认本 master plan 后,我会立刻为 **P1-0 (Monorepo 骨架)** 写详细 TDD plan,大概 8-12 个步骤,涵盖:

- 目录拆分(把现有 `src/` 移到 `backend/src/`)
- 父 POM + backend pom.xml 拆分
- 新建 `desktop/build.gradle.kts` + `settings.gradle.kts` 骨架
- 验证 `mvn spring-boot:run` 和 `gradle :desktop:assemble` 都能跑
- `.gitignore` / README 同步更新
- 第一个 git commit "chore: monorepo skeleton"

之后 P1-1 → P1-4 同样的节奏:**写细 plan → 你确认 → 执行 → 验收 → 下一阶段**。

---

## Open Questions (已决策 ✅ 2026-05-21)

| # | 问题 | 决策 |
|---|---|---|
| Q1 | Backend package 命名 | **`com.wzx.babiq.server`** |
| Q2 | Desktop package 命名 | **`com.wzx.babiq.desktop`** |
| Q3 | §1 ✅/❌ 范围调整 | **不调整** |
| Q4 | M0-M4 拆分顺序 | **OK,按现有方案** |
| Q5 | Monorepo 形态 | **根目录只放文档+git,`backend/` 和 `desktop/` 各自是完全独立的项目**(不引父 POM 聚合,符合 YAGNI) |
