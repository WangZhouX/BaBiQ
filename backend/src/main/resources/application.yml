spring:
  application:
    name: BaBiQ-Server

server:
  port: 8080

logging:
  level:
    com.wzx.babiq.server: DEBUG

babiq:
  ws:
    path: /ws/agent
    allowed-origins: "*"
  protocol:
    mock-agent-text: "hello from babiq"
