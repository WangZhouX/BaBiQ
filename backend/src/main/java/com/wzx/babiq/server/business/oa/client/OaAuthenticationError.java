package com.wzx.babiq.server.business.oa.client;

public enum OaAuthenticationError {
    INVALID_PASSWORD_FORMAT,
    INVALID_CREDENTIALS,
    ACCOUNT_NOT_FOUND,
    AUTH_EXPIRED,
    MEMBER_EXPIRED,
    REMOTE_PROTOCOL_ERROR,
    REMOTE_UNAVAILABLE,
    REMOTE_TIMEOUT
}
